<header class="site-header" data-section-id="{{ section.id }}">
  {%- if section.settings.show_announcement and section.settings.announcement_text != blank -%}
    <div class="announcement-bar">
      <div class="announcement-bar__inner page-width">
        {%- if section.settings.announcement_link != blank -%}
          <a href="{{ section.settings.announcement_link }}">{{ section.settings.announcement_text }}</a>
        {%- else -%}
          <p>{{ section.settings.announcement_text }}</p>
        {%- endif -%}
      </div>
    </div>
  {%- endif -%}

  <div class="site-header__inner page-width">
    <button class="icon-button site-header__menu-button" type="button" data-drawer-open="menu-drawer" aria-controls="menu-drawer" aria-expanded="false">
      <span class="icon-button__lines" aria-hidden="true"></span>
      <span class="visually-hidden">{{ 'sections.header.open_menu' | t }}</span>
    </button>

    <a class="site-header__logo" href="{{ routes.root_url }}" aria-label="{{ shop.name }}">
      {%- if section.settings.logo != blank -%}
        {{ section.settings.logo | image_url: width: 220 | image_tag: widths: '110, 160, 220', loading: 'eager', alt: shop.name }}
      {%- else -%}
        <span class="site-header__wordmark">Premium Diamond</span>
      {%- endif -%}
    </a>

    {%- if section.settings.menu != blank -%}
      <nav class="site-header__nav" aria-label="{{ 'sections.header.navigation' | t }}">
        {%- for link in section.settings.menu.links -%}
          <div class="site-header__nav-item">
            <a href="{{ link.url }}">{{ link.title }}</a>
            {%- if link.links != blank -%}
              <div class="site-header__submenu">
                <div class="site-header__submenu-inner page-width">
                  <div class="site-header__submenu-feature">
                    <p class="eyebrow">{{ section.settings.mega_eyebrow }}</p>
                    <strong>{{ section.settings.mega_heading }}</strong>
                    <span>{{ section.settings.mega_text }}</span>
                  </div>
                  <div class="site-header__submenu-links">
                    {%- for child_link in link.links -%}
                      <a href="{{ child_link.url }}">{{ child_link.title }}</a>
                    {%- endfor -%}
                  </div>
                </div>
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </nav>
    {%- endif -%}

    <div class="site-header__actions">
      <button class="icon-button site-header__search-trigger" type="button" data-drawer-open="search-drawer" aria-controls="search-drawer" aria-expanded="false">
        <span class="svg-wrapper" aria-hidden="true">
          <svg viewBox="0 0 20 20" fill="none"><circle cx="9" cy="9" r="4.75" stroke="currentColor" stroke-width="1.4"/><path d="m12.5 12.5 3.25 3.25" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.4"/></svg>
        </span>
        <span class="site-header__search-text">Search</span>
        <span class="visually-hidden">{{ 'general.search.search' | t }}</span>
      </button>
      {%- if shop.customer_accounts_enabled -%}
        <button class="icon-button site-header__account-trigger" type="button" data-drawer-open="account-drawer" aria-controls="account-drawer" aria-expanded="false" aria-label="Account">
          <span class="svg-wrapper" aria-hidden="true">
            <svg viewBox="0 0 20 20" fill="none"><circle cx="10" cy="6.25" r="3.25" stroke="currentColor" stroke-width="1.4"/><path d="M3.75 17.25c.55-3.05 2.78-5 6.25-5s5.7 1.95 6.25 5" stroke="currentColor" stroke-linecap="round" stroke-width="1.4"/></svg>
          </span>
          <span class="site-header__account-text">{% if customer %}Profile{% else %}Login{% endif %}</span>
        </button>
      {%- endif -%}
      <button class="icon-button" type="button" data-cart-trigger data-drawer-open="cart-drawer" aria-controls="cart-drawer" aria-expanded="false" aria-label="{{ 'templates.cart.cart' | t }}">
        <span class="svg-wrapper" aria-hidden="true">
          <svg viewBox="0 0 20 20" fill="none"><path d="M3.392 6.875h13.216v8.016c0 .567-.224 1.112-.624 1.513-.4.402-.941.627-1.506.627H5.522a2.13 2.13 0 0 1-1.506-.627 2.15 2.15 0 0 1-.624-1.513zM8.818 2.969h2.333c.618 0 1.211.247 1.649.686a2.35 2.35 0 0 1 .683 1.658v1.562H6.486V5.313c0-.622.246-1.218.683-1.658a2.33 2.33 0 0 1 1.65-.686" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.4"/></svg>
        </span>
        <span class="cart-bubble" aria-hidden="true" data-cart-count>{{ cart.item_count }}</span>
      </button>
    </div>
  </div>

  <div class="drawer" id="menu-drawer" data-drawer hidden>
    <div class="drawer__overlay" data-drawer-close></div>
    <div class="drawer__panel" role="dialog" aria-modal="true" aria-label="{{ 'sections.header.mobile_menu' | t }}" tabindex="-1">
      <div class="drawer__header">
        <strong>{{ shop.name }}</strong>
        <button class="icon-button" type="button" data-drawer-close>
          <span aria-hidden="true">Close</span>
          <span class="visually-hidden">{{ 'accessibility.close' | t }}</span>
        </button>
      </div>

      {%- if section.settings.menu != blank -%}
        <nav class="drawer__nav" aria-label="{{ 'sections.header.mobile_menu' | t }}">
          {%- for link in section.settings.menu.links -%}
            {%- if link.links != blank -%}
              <details>
                <summary>{{ link.title }}</summary>
                <div class="drawer__subnav">
                  <a href="{{ link.url }}">{{ 'sections.header.view_all' | t: title: link.title }}</a>
                  {%- for child_link in link.links -%}
                    <a href="{{ child_link.url }}">{{ child_link.title }}</a>
                  {%- endfor -%}
                </div>
              </details>
            {%- else -%}
              <a href="{{ link.url }}">{{ link.title }}</a>
            {%- endif -%}
          {%- endfor -%}
        </nav>
      {%- endif -%}
    </div>
  </div>

  <div class="drawer drawer--right" id="search-drawer" data-drawer hidden>
    <div class="drawer__overlay" data-drawer-close></div>
    <div class="drawer__panel premium-search flowbit-search" role="dialog" aria-modal="true" aria-label="{{ 'general.search.search' | t }}" tabindex="-1">
      <form action="{{ routes.search_url }}" method="get" role="search" class="flowbit-search__form" data-predictive-search-form>
        <div class="flowbit-search__header">
          <label for="FlowbitSearchInput-{{ section.id }}" class="visually-hidden">{{ 'general.search.search' | t }}</label>
          <span class="flowbit-search__icon" aria-hidden="true">
            <svg viewBox="0 0 20 20" fill="none"><circle cx="9" cy="9" r="4.75" stroke="currentColor" stroke-width="1.4"/><path d="m12.5 12.5 3.25 3.25" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.4"/></svg>
          </span>
          <input id="FlowbitSearchInput-{{ section.id }}" class="flowbit-search__input" type="search" name="q" autocomplete="off" placeholder="Search" data-predictive-search-input>
          <button class="flowbit-search__reset" type="button" data-search-reset hidden>Clear</button>
          <button class="flowbit-search__close" type="button" data-drawer-close aria-label="{{ 'accessibility.close' | t }}">
            <span aria-hidden="true">
              <svg viewBox="0 0 14 14" fill="none"><path d="M12 2 2 12M12 12 2 2" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>
            </span>
          </button>
        </div>

        <div class="flowbit-search__content">
          <aside class="flowbit-search__discovery" data-search-discovery>
            <section class="flowbit-search__panel flowbit-search__panel--terms">
              <div class="flowbit-search__section-head"><h3>Popular Search Terms</h3></div>
              <div class="flowbit-search__term-list">
                {%- assign popular_terms = section.settings.popular_search_terms | split: "," -%}
                {%- for term in popular_terms -%}
                  {%- assign clean_term = term | strip -%}
                  {%- if clean_term != blank -%}
                    <button type="button" data-search-term="{{ clean_term | escape }}">{{ clean_term }}</button>
                  {%- endif -%}
                {%- endfor -%}
              </div>
            </section>

            <section class="flowbit-search__panel flowbit-search__panel--categories">
              <div class="flowbit-search__section-head"><h3>Shop by Category</h3></div>
              <div class="flowbit-search__category-list">
                <a href="{{ routes.all_products_collection_url }}">All products</a>
                <a href="{{ routes.collections_url }}">Collections</a>
                <button type="button" data-search-term="tops">Tops</button>
                <button type="button" data-search-term="bottoms">Bottoms</button>
                <button type="button" data-search-term="accessories">Accessories</button>
              </div>
            </section>
          </aside>

          <section class="flowbit-search__panel" data-search-suggestions hidden>
            <div class="flowbit-search__section-head"><h3>Suggestions</h3></div>
            <div class="flowbit-search__suggestions" data-search-suggestions-list></div>
          </section>

          <section class="flowbit-search__panel" data-previous-searches hidden>
            <div class="flowbit-search__section-head">
              <h3>Recent</h3>
              <button type="button" data-clear-searches>Clear</button>
            </div>
            <div class="premium-search__chips" data-previous-searches-list></div>
          </section>

          <section class="flowbit-search__panel" data-predictive-search-results hidden>
            <div class="flowbit-search__section-head"><h3>Products</h3></div>
            <div class="flowbit-search__results" data-predictive-search-list></div>
          </section>

          <section class="flowbit-search__panel" data-search-recommendations>
            <div class="flowbit-search__section-head"><h3>Popular Right Now</h3></div>
            <div class="flowbit-search__results">
              {%- if section.settings.search_products != blank -%}
                {%- for product in section.settings.search_products limit: 4 -%}
                  <article class="flowbit-search-card">
                    <a href="{{ product.url }}" class="flowbit-search-card__link">
                      <span class="visually-hidden">{{ product.title }}</span>
                    </a>
                    <div class="flowbit-search-card__media">
                      {%- if product.featured_image -%}
                        {{ product.featured_image | image_url: width: 500 | image_tag: widths: '240, 352, 500', loading: 'lazy', alt: product.title }}
                      {%- endif -%}
                    </div>
                    <p>{{ product.title }}</p>
                  </article>
                {%- endfor -%}
              {%- else -%}
                {%- for product in collections.all.products limit: 4 -%}
                  <article class="flowbit-search-card">
                    <a href="{{ product.url }}" class="flowbit-search-card__link">
                      <span class="visually-hidden">{{ product.title }}</span>
                    </a>
                    <div class="flowbit-search-card__media">
                      {%- if product.featured_image -%}
                        {{ product.featured_image | image_url: width: 500 | image_tag: widths: '180, 320, 500', loading: 'lazy', alt: product.title }}
                      {%- endif -%}
                    </div>
                    <p>{{ product.title }}</p>
                  </article>
                {%- else -%}
                  <article class="flowbit-search-card">
                    <a href="{{ routes.all_products_collection_url }}" class="flowbit-search-card__link"><span class="visually-hidden">Shop all</span></a>
                    <div class="flowbit-search-card__media"></div>
                    <p>Add products in Shopify admin to power search recommendations.</p>
                  </article>
                {%- endfor -%}
              {%- endif -%}
            </div>
          </section>
        </div>

        <div class="flowbit-search__footer">
          <button class="button button-primary flowbit-search__view-all" type="submit">View all</button>
        </div>
      </form>
    </div>
  </div>

  <div class="drawer drawer--right" id="cart-drawer" data-drawer hidden>
    <div class="drawer__overlay" data-drawer-close></div>
    <div class="drawer__panel cart-drawer premium-cart" role="dialog" aria-modal="true" aria-label="{{ 'templates.cart.cart' | t }}" tabindex="-1">
      <div class="drawer__header premium-drawer-header">
        <div>
          <span class="premium-drawer-kicker">Bag</span>
          <strong>{{ 'templates.cart.cart' | t }}</strong>
        </div>
        <button class="icon-button premium-close" type="button" data-drawer-close>
          <span aria-hidden="true">Close</span>
          <span class="visually-hidden">{{ 'accessibility.close' | t }}</span>
        </button>
      </div>

      <div class="cart-drawer__body" data-cart-drawer-body>
        <div class="cart-drawer__items" data-cart-drawer-items>
          {%- if cart.empty? -%}
            <div class="empty-state cart-drawer__empty premium-cart-empty">
              <p>{{ 'templates.cart.empty' | t }}</p>
              <a class="button" href="{{ routes.all_products_collection_url }}">{{ 'general.continue_shopping' | t }}</a>
            </div>
          {%- else -%}
            {%- for item in cart.items -%}
              <article class="cart-drawer__item premium-cart-item">
                <a class="cart-drawer__image" href="{{ item.url }}">
                  {%- if item.image -%}
                    {{ item.image | image_url: width: 220 | image_tag: widths: '90, 140, 220', loading: 'lazy', alt: item.product.title }}
                  {%- endif -%}
                </a>
                <div>
                  <a class="cart-drawer__title" href="{{ item.url }}">{{ item.product.title }}</a>
                  {%- unless item.product.has_only_default_variant -%}
                    <p>{{ item.variant.title }}</p>
                  {%- endunless -%}
                  <p>{{ 'products.product.quantity' | t }}: {{ item.quantity }}</p>
                </div>
                <strong>{{ item.final_line_price | money }}</strong>
              </article>
            {%- endfor -%}
          {%- endif -%}
        </div>

        {%- if section.settings.cart_products != blank -%}
          <section class="premium-cart-recommendations">
            <div class="premium-search__section-head"><span>{{ section.settings.cart_recommendations_title }}</span></div>
            <div class="premium-cart-recommendations__grid">
              {%- for product in section.settings.cart_products limit: 3 -%}
                <article class="premium-search-product premium-search-product--compact">
                  <a href="{{ product.url }}" class="premium-search-product__media">
                    {%- if product.featured_image -%}
                      {{ product.featured_image | image_url: width: 180 | image_tag: widths: '90, 140, 180', loading: 'lazy', alt: product.title }}
                    {%- endif -%}
                  </a>
                  <div>
                    <a href="{{ product.url }}" class="premium-search-product__title">{{ product.title }}</a>
                    <span>{{ product.price | money }}</span>
                    {%- if product.available and product.has_only_default_variant -%}
                      <product-form>
                        {%- form 'product', product, class: 'premium-search-product__form' -%}
                          <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
                          <input type="hidden" name="quantity" value="1">
                          <button class="premium-mini-button" type="submit" data-add-to-cart>Add</button>
                        {%- endform -%}
                      </product-form>
                    {%- else -%}
                      <a class="premium-mini-button" href="{{ product.url }}">View</a>
                    {%- endif -%}
                  </div>
                </article>
              {%- endfor -%}
            </div>
          </section>
        {%- endif -%}
      </div>

      <form class="cart-drawer__footer premium-cart-footer" action="{{ routes.cart_url }}" method="post" data-cart-drawer-footer {% if cart.empty? %}hidden{% endif %}>
        <label class="form-field">
          <span>{{ 'templates.cart.note' | t }}</span>
          <textarea name="note" rows="3">{{ cart.note }}</textarea>
        </label>
        <div class="cart-drawer__subtotal">
          <span>{{ 'templates.cart.subtotal' | t }}</span>
          <strong data-cart-subtotal>{{ cart.total_price | money }}</strong>
        </div>
        <button class="button" type="submit" name="checkout">{{ 'templates.cart.checkout' | t }}</button>
        <a class="button button--secondary" href="{{ routes.cart_url }}">{{ 'templates.cart.view_cart' | t }}</a>
      </form>
    </div>
  </div>

  {%- if shop.customer_accounts_enabled -%}
    <div class="drawer drawer--right" id="account-drawer" data-drawer hidden>
      <div class="drawer__overlay" data-drawer-close></div>
      <div class="drawer__panel premium-account" role="dialog" aria-modal="true" aria-label="Account" tabindex="-1">
        <div class="drawer__header premium-drawer-header">
          <div>
            <span class="premium-drawer-kicker">Profile</span>
            <strong>{% if customer %}{{ customer.first_name | default: 'Account' }}{% else %}Account{% endif %}</strong>
          </div>
          <button class="icon-button premium-close" type="button" data-drawer-close>
            <span aria-hidden="true">Close</span>
            <span class="visually-hidden">{{ 'accessibility.close' | t }}</span>
          </button>
        </div>

        <div class="premium-account__body">
          {%- if customer -%}
            <div class="premium-account__card">
              <span class="premium-account__avatar">{{ customer.first_name | default: customer.email | slice: 0 | upcase }}</span>
              <div>
                <strong>{{ customer.name | default: customer.email }}</strong>
                <p>{{ customer.email }}</p>
              </div>
            </div>
            <div class="premium-account__stats">
              <div><span>Orders</span><strong>{{ customer.orders_count }}</strong></div>
              <div><span>Total spent</span><strong>{{ customer.total_spent | money }}</strong></div>
              <div><span>Status</span><strong>Member</strong></div>
            </div>
            <nav class="premium-account__links" aria-label="Account links">
              <a href="{{ routes.account_url }}">Dashboard</a>
              <a href="{{ routes.account_addresses_url }}">Addresses</a>
              <a href="{{ routes.cart_url }}">Bag</a>
              <a href="{{ routes.account_logout_url }}">Logout</a>
            </nav>
          {%- else -%}
            <div class="premium-account__intro">
              <p class="eyebrow">Premium Diamond member area</p>
              <h2>Track orders, save details, and check out faster.</h2>
            </div>
            <div class="premium-account__actions">
              <a class="button" href="{{ routes.account_login_url }}">Login</a>
              <a class="button button--secondary" href="{{ routes.account_register_url }}">Create account</a>
            </div>
            <div class="premium-account__perks">
              <span>Order history</span>
              <span>Saved addresses</span>
              <span>Faster checkout</span>
              <span>Drop access ready</span>
            </div>
          {%- endif -%}
        </div>
      </div>
    </div>
  {%- endif -%}
</header>

{% schema %}
{
  "name": "Header",
  "settings": [
    { "type": "image_picker", "id": "logo", "label": "Logo" },
    { "type": "link_list", "id": "menu", "label": "Menu", "default": "main-menu" },
    { "type": "checkbox", "id": "show_announcement", "label": "Show announcement bar", "default": true },
    { "type": "text", "id": "announcement_text", "label": "Announcement text", "default": "Premium diamond goods / fast dispatch / limited drops" },
    { "type": "url", "id": "announcement_link", "label": "Announcement link" },
    { "type": "text", "id": "mega_eyebrow", "label": "Mega menu eyebrow", "default": "Curated edit" },
    { "type": "text", "id": "mega_heading", "label": "Mega menu heading", "default": "Shop the season" },
    { "type": "textarea", "id": "mega_text", "label": "Mega menu text", "default": "Use nested menu links to create a polished navigation experience for collections, campaigns, and product categories." },
    { "type": "text", "id": "search_placeholder", "label": "Search label", "default": "Search the collection" },
    { "type": "text", "id": "search_input_placeholder", "label": "Search input placeholder", "default": "Type product, color, style..." },
    { "type": "text", "id": "search_recommendations_title", "label": "Search recommendations title", "default": "Recommended for you" },
    { "type": "textarea", "id": "popular_search_terms", "label": "Popular search terms", "default": "tops, bottoms, accessories, new drops, socks, black, red, oversized" },
    { "type": "product_list", "id": "search_products", "label": "Search recommended products", "limit": 6 },
    { "type": "text", "id": "cart_recommendations_title", "label": "Cart recommendations title", "default": "Complete the look" },
    { "type": "product_list", "id": "cart_products", "label": "Cart recommended products", "limit": 3 }
  ]
}
{% endschema %}
